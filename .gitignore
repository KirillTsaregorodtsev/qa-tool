# secrets — НИКОГДА не коммитить
volume/config/app.env
volume/config/ssh_key
volume/config/settings.json

# deps & build
node_modules/
**/frontend/dist/

# python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# IDE / editors
.idea/
.vscode/
*.swp
*~

# runtime data — keep the dirs (.gitkeep), ignore their contents
volume/reports/*
!volume/reports/.gitkeep
volume/cache/*
!volume/cache/.gitkeep

# local tooling — personal Claude settings only; shared .claude/settings.json is committed
.claude/settings.local.json
.entire/
*.backup
tmp/

# personal per-developer progress file — never committed
AGENT_TEAM_STATE.md
.DS_Store
